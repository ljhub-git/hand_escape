using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class LoginManager : MonoBehaviour
{
    [SerializeField]
    private TextMeshProUGUI idInputField;
    [SerializeField]
    private TextMeshProUGUI passwordInputField;
    [SerializeField]
    private TextMeshProUGUI keyboardInput;
    [SerializeField]
    private Button loginButton;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
